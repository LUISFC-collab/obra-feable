destrabar Pages -> c44 (cargas ya reportadas: DOS toggles por accion en Config, como pidio Luis. Cada una de modificar/borrar/cambiar-fecha de una carga tiene ahora: 🟡 SOLICITAR (el rol pide y el admin aprueba) y 🟢 CAMBIO DIRECTO (el rol lo hace sin pedir, como admin). El 'directo' viene APAGADO por defecto (el admin lo asigna a quien quiera). Enforcement: admin o rol con *_dir => directo; si no, rol con el de solicitar => solicitud; si no => bloqueado. Ademas se implemento el cambio de fecha DIRECTO (antes ni el admin lo hacia directo, siempre solicitaba). 3 llaves nuevas: act_mod_carga_dir, act_borrar_carga_dir, act_fecha_carga_dir.)

Water Transition II - Carachugo 1-9. PWA de una sola pagina (index.html + sw.js + version.txt) en GitHub Pages + Supabase.
